apiVersion: v1
kind: Service
metadata:
  name: store-db
  namespace: eatcloud
  labels:
    app: store-db
spec:
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: store-db
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: store-db
  namespace: eatcloud
spec:
  replicas: 1
  selector:
    matchLabels:
      app: store-db
  template:
    metadata:
      labels:
        app: store-db
    spec:
      containers:
      - name: postgres
        image: postgis/postgis:15-3.3-alpine  # ✅ PostGIS 지원 이미지로 변경
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: "store_db"
        - name: POSTGRES_USER
          value: "eatcloud_user"
        - name: POSTGRES_PASSWORD
          value: "devpassword123"
        - name: PGDATA
          value: "/var/lib/postgresql/data/pgdata"
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - eatcloud_user
            - -d
            - store_db
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 5
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - eatcloud_user
            - -d
            - store_db
          initialDelaySeconds: 60
          periodSeconds: 15
          timeoutSeconds: 10
          failureThreshold: 5
      volumes:
      - name: postgres-data
        emptyDir: {}
